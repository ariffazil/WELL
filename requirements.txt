# requirements.txt — WELL (well-mcp): MCP-relevant DIRECT dependencies
#
# CREATED 2026-09-15 (KVM8) — WELL had NO requirements.txt.
#
# AUTHORITY: pyproject.toml + uv.lock remain the SOLE resolver authority for WELL
# (see .github/dependabot.yml and .github/workflows/ci-uv-lock-invariant.yml).
# This file is an informational freeze of the MCP-relevant direct dependencies AS
# INSTALLED in /root/WELL/.venv on 2026-09-15. Deliberately NOT a full venv freeze.
#
# ⚠ WELL IS THE FEDERATION OUTLIER ON THE MCP STACK — recorded here truthfully
#   as-installed rather than as-hopefully-declared:
#     WELL          mcp 1.28.1, fastmcp 3.4.4, fastmcp-slim 3.4.4
#     arifOS / GEOX mcp 1.29.0, fastmcp 3.4.6, fastmcp-slim 3.4.6
#     WEALTH        mcp 1.29.0, fastmcp 3.4.5, fastmcp-slim 3.4.5
#   WELL is one mcp minor (1.28.1 vs 1.29.0) and two fastmcp patches (3.4.4 vs
#   3.4.6) behind. Its pyproject.toml declares `fastmcp[tasks]==3.4.7` and its
#   uv.lock resolves mcp 1.29.1 / fastmcp 3.4.7 — NEITHER is installed.
#   Converging WELL onto the federation line is an INSTALL change and is
#   deliberately NOT performed by this declaration reconciliation.
#
# Evidence: WELL source imports FastMCP, Context, fastmcp.exceptions.ToolError,
# fastmcp.prompts.base.Message, fastmcp.server.dependencies.get_http_request,
# fastmcp.server.middleware.Middleware, mcp.server.fastmcp.FastMCP,
# mcp.server.streamable_http.StreamableHTTPServerTransport and mcp.types.*
# — all 11 symbols resolve against the versions pinned below.
# WELL/server.py patches StreamableHTTPServerTransport._check_accept_headers with
# a getattr/None guard, so the code is correct under both mcp 1.x and 2.x.
#
# fastapi is intentionally ABSENT: WELL neither declares nor installs it.
mcp==1.28.1
fastmcp==3.4.4
fastmcp-slim==3.4.4
pydantic==2.13.4
uvicorn==0.49.0
starlette==1.6.0
